[project]
name = "modulo2-2"
version = "0.1.0"
description = ""
authors = [
    {name = "MarcosMarquezOvalle",email = "mmarquez17@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
]

[tool.poetry]
packages = [{include = "modulo2_2", from = "src"}]

[tool.pytest.ini_options]
pythonpath = ["src"]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
